import * as vscode from "vscode";

// This interface is intentionally a typed disposable marker.
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface FeatureCommands extends vscode.Disposable {}
